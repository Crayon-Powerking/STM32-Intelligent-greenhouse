# STM32-Intelligent-greenhouse
基于STM32运行的裸机代码，通过收集温湿度传感器的信息和光敏传感器的信息，然后通过中断来对信息进行判断（是否超出设定监测阈值），然后单片机控制对应模块进行处理，例如亮灯补光和风扇降温达到调节作用（没有湿度的负反馈调节，因为当时不想花钱了），主函数负责实时打印传感器数据通过oled显示，按键一进行对光照，温度，湿度的阈值进行设定，通过旋转编码器控制对应阈值的大小。按键二实现伪休眠状态，清除oled显示同时切换单片机的工作mode。整体系统设计通过状态机来控制对不同状况的处理。
